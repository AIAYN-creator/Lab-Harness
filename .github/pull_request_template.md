## What and why

<!-- What does this change do, and why is it needed? Link the issue it closes. -->

Closes #

## Checklist

- [ ] The title follows Conventional Commits (e.g. `feat(plots): add power-law fits`).
- [ ] Tests cover the change, and `uv run pytest` passes locally.
- [ ] Docs and `docs/CHANGELOG.md` are updated if behaviour changed.
- [ ] No fonts, sizes or colours are hard-coded (everything comes from the shared style).
- [ ] The core still imports without any extra installed.
- [ ] The change respects the accepted ADRs in `docs/adr/`.
